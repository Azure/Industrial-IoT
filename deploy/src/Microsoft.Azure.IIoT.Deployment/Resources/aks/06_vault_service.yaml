apiVersion: v1
kind: Service
metadata:
  name: vault-service
  namespace: industrial-iot
  labels:
    app: vaultservice
spec:
  type: ClusterIP
  selector:
    app: vaultservice
  ports:
  - port: 443
    protocol: TCP
    name: https
  - port: 9044
    protocol: TCP
    name: rest
