assembly-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery
branches:
  develop:
    regex: ^master$
    tag: alpha
  master:
    regex: ^stable$
  release:
    regex: ^staging[/-]
    tag: prerelease
