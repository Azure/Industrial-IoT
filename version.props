<Project>
  <PropertyGroup>
    <VersionPrefix>2.0.1</VersionPrefix>
    <VersionSuffix>prerelease-$([System.DateTime]::Now.ToString("yyyyMMdd"))</VersionSuffix>
    <VersionSuffix Condition="'$(Build.SourceBranchName)' == 'master'"></VersionSuffix>
  </PropertyGroup>
</Project>
