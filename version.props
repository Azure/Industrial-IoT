<Project>
  <PropertyGroup>
    <VersionPrefix>2.0.2</VersionPrefix>
    <VersionSuffix Condition="'$(RELEASE_BUILD)' == ''">prerelease-$([System.DateTime]::Now.ToString("yyyyMMdd"))</VersionSuffix>
  </PropertyGroup>
</Project>
